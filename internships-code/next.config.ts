import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove the 'eslint' property
  reactStrictMode: true,
  // Add any other configurations i need later

};

export default nextConfig;