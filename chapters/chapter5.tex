\chapter{Methodology and System Overview}
This chapter provides the architectural blueprint for the lightweight semantic structuring system. By leveraging the Next.js App Router and a modular TypeScript backend, the system achieves a performant data pipeline that avoids the latency of external LLM dependencies while maintaining high extraction precision.

\section{Functional Requirements and Use Cases}
The system is designed to serve two primary actors: the \textit{Student/Job Seeker}, who interacts with the search interface to discover opportunities, and the \textit{System Administrator/Data Engineer}, who manages the automated ingestion and maintenance of the data pipeline. The following Use Case Diagram illustrates the high-level functional requirements and the boundaries of the system.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{uml/useCaseDiagram.png}
    \caption{UML Use Case Diagram: Actor Interactions and System Scope}
    \label{fig:use_case_diagram}
\end{figure}

\section{System Architecture and Component Mapping}
The system is organized into four logical tiers. The following UML Component Diagram illustrates the physical organization of the \texttt{internships-code} directory and the dependencies between modules, specifically highlighting the interaction between the file-based storage, the server-side logic, and the React client components.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=2.5cm,
        package/.style={rectangle, draw, thick, fill=gray!5, text width=3.8cm, align=left, minimum height=1.5cm, copy shadow={shadow xshift=0.1cm, shadow yshift=-0.1cm}},
        file/.style={rectangle, draw, fill=white, text width=3.4cm, align=left, font=\ttfamily\scriptsize},
        arrow/.style={-stealth, thick, dashed}
    ]
        % Packages
        \node (public) [package] {\textbf{public/} \\ \scriptsize (Static Data Assets)};
        \node (lib) [package, right of=public, xshift=3.8cm] {\textbf{app/lib/} \\ \scriptsize (Data Processing Layer)};
        \node (api) [package, below of=lib, yshift=-0.5cm] {\textbf{app/api/internship/} \\ \scriptsize (REST Endpoints)};
        \node (components) [package, right of=lib, xshift=4.2cm] {\textbf{app/components/} \\ \scriptsize (UI/Client Layer)};

        % Files inside packages
        \node (json) [file, below of=public, yshift=1.5cm, xshift=0.3cm] {intern\_data.json};
        \node (dataTs) [file, below of=lib, yshift=1.5cm, xshift=0.3cm] {data.ts};
        \node (supabase) [file, below of=dataTs, yshift=1.8cm] {supabase-client.ts};
        \node (route) [file, below of=api, yshift=1.5cm, xshift=0.3cm] {route.ts};
        \node (ui) [file, below of=components, yshift=1.5cm, xshift=0.3cm] {InternshipList.tsx};

        % Relationships
        \draw [arrow] (dataTs) -- (json) node[midway, above, font=\tiny] {fs.readFile};
        \draw [arrow] (route) -- (dataTs) node[midway, right, font=\tiny] {getProcessedData};
        \draw [arrow] (ui) -- (dataTs) node[midway, above, font=\tiny] {Interface Import};
        \draw [arrow] (ui) -- (route) node[midway, right, xshift=1.2cm, yshift=-0.4cm, font=\tiny] {Client Fetch (GET)};
        \draw [arrow] (dataTs) -- (supabase) node[midway, left, font=\tiny] {Database Client};
    \end{tikzpicture}
    \caption{UML Component Diagram: Mapping Logical Layers to the \texttt{internships-code} Project Structure}
    \label{fig:component_diagram}
\end{figure}

\section{Core Data Model}
The semantic integrity of the pipeline is maintained by the \texttt{Internship} interface defined in \texttt{app/lib/data.ts}. This model acts as the formal contract between the ingestion logic and the frontend presentation layer.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{uml/classdiagram.png}
    \caption{UML Class Representation of the \texttt{Internship} Semantic Schema}
    \label{fig:internship_class}
\end{figure}

\section{The Ingestion and Retrieval Flow}
The interaction between the client-side UI and the server-side data processing layer is managed through asynchronous requests. The following sequence diagram illustrates the lifecycle of a data request, from the initial user visit to the delivery of processed semantic entities.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{uml/sequenceDiagramFrontend.png}
    \caption{UML Sequence Diagram: Data Retrieval and API Interaction}
    \label{fig:sequence_diagram}
\end{figure}

\section{The Ingestion Pipeline}
The data ingestion process, implemented in \texttt{app/lib/data.ts} and mirrored in the API layer, follows a sequence of operations to ensure raw JSON data is transformed into structured entities.

\begin{enumerate}
    \item \textbf{I/O and Safety Validation:} The system reads \texttt{intern\_data.json} using \texttt{promises/fs}. It performs safety checks to ensure the file is not empty and does not contain unexpected HTML error responses.
    \item \textbf{Cleaning and Normalization:}
        \begin{itemize}
            \item \textbf{Text Fields:} Uses \texttt{cleanTitle}, \texttt{cleanCompany}, and \texttt{cleanLocation} to handle missing values and whitespace.
            \item \textbf{Salaries:} Employs specialized regex to isolate compensation values from noisy text.
        \end{itemize}
    \item \textbf{ID Generation and Conflict Resolution:} The \texttt{generateId} function creates slugs. If multiple listings share the same title and company, a sequential suffix is appended to ensure uniqueness.
    \item \textbf{Semantic Mapping:}
        \begin{itemize}
            \item \textbf{Skills:} Case-insensitive matching against a dictionary of technical keywords.
            \item \textbf{Categories:} Heuristics mapping roles to domains such as \textit{Software}, \textit{Data/AI}, or \textit{Healthcare}.
        \end{itemize}
    \item \textbf{State Management:} Processed results are stored in a \texttt{cachedInternships} variable with a 300,000ms (5-minute) TTL to optimize server response times.
\end{enumerate}
