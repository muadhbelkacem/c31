\chapter{Results and Comparative Evaluation}

\section{Chapter Introduction}

This chapter presents an evaluation of the lightweight semantic enrichment pipeline developed in this thesis. Its purpose is to examine what the current evidence shows about noisy scraped internship data, semantic structuring, internship detection, structured discovery, uncertainty handling, and local operational feasibility. Technical measurements are therefore treated as supporting feasibility source, not as the main research contribution.

The evaluation is connected to the research questions defined in Chapter 1. RQ1 is addressed through the observed noise, fake roles, and ambiguity in the inspected scraped-role sample. RQ2 is addressed through the structured records produced by the enrichment pipeline, including normalized metadata, extracted skills, internship probabilities, and review indicators. RQ3 is addressed through frontend search, filtering, and response evidence, while avoiding unsupported claims about formal user satisfaction. RQ4 is addressed through the comparison between human review, ChatGPT5.5-assisted review, and Transformer-based internship detection. RQ5 is addressed through local build, file-size, response-time evidence, and the limitations of the evaluation.

All numerical results used in this chapter are limited to the current Chapter 6 supplied classification statistics. The chapter therefore focuses on defensible findings: whether the pipeline can structure noisy recruitment data, support internship detection, preserve uncertainty, and enable efficient structured discovery within the evaluated scope.

\section{Evaluation Evidence and Dataset Scope}

The evaluation uses a bounded evidence set. Rather than treating the whole implemented project as a
source of unrestricted numerical claims, the chapter relies on specific proof files that record classification
comparison, frontend dataset statistics, local build results, local response speed, and frontend search/filter
timing.

\subsection{Evidence Files Used}

\begin{itemize}
    \item \texttt{LLM-HUMAN-TRANSFORMER.txt}
    \item \texttt{website\_data\_statistics.json}
    \item \texttt{website\_data\_statistics.csv}
    \item \texttt{frontend\_build\_performance\_actual.json}
    \item \texttt{frontend\_build\_performance\_actual.csv}
    \item \texttt{frontend\_response\_speed\_actual.json}
    \item \texttt{frontend\_response\_speed\_actual.csv}
    \item \texttt{frontend\_search\_filter\_summary.json}
    \item \texttt{frontend\_search\_filter\_summary.csv}
    \item \texttt{raw\_frontend\_build\_stdout.log}
    \item \texttt{raw\_frontend\_build\_time\_and\_warnings.log}
    \item \texttt{raw\_frontend\_http\_samples.csv}
    \item \texttt{raw\_frontend\_search\_filter\_samples.csv}
    \item \texttt{raw\_frontend\_server\_runtime.log}
    \item \texttt{source\_manifest.json}
\end{itemize}

\subsection{Evaluation Sample}

The classification evaluation is conducted on a sample of 300 scraped roles. Human review is treated as
the reference interpretation for this sample. The selected classification identifies 128 roles as real
internships, 9 as ambiguous cases, and 163 as fake or non-internship roles. This distribution shows that the
inspected data contains a substantial mixture of valid internships, uncertain opportunities, and irrelevant or
non-internship records.

The 300-role sample is therefore suitable for evaluating the thesis problem because it reflects the central
difficulty of internship aggregation: records collected from general recruitment sources cannot be assumed
to be genuine internships simply because they were retrieved by internship-oriented scraping or search
terms.

\subsection{Evaluation Boundaries}

The results in this chapter must be interpreted within several boundaries. First, the classification results are
sample-based and should not be generalized as universal model accuracy. Second, ambiguous cases
remain difficult to automate because they often depend on missing context, employer terminology, or
incomplete descriptions. Third, no formal user study was conducted; therefore, this chapter evaluates
structured discovery behavior rather than user satisfaction. Fourth, frontend timing results are local
measurements observed in the tested environment and should not be treated as universal cloud-deployment
guarantees. Fifth, files containing older or inconsistent classification distributions were excluded from the
quantitative argument to preserve a single coherent basis.


\section{Dataset Noise and Internship-Posting Heterogeneity Results (RQ1)}

RQ1 asks what structural, lexical, and semantic inconsistencies characterize heterogeneous online
internship postings, and how these inconsistencies affect internship-oriented representation and discovery.
The 300-role classification sample directly supports this question by showing that scraped recruitment data
contains both genuine internships and many records that should not be accepted as internships.

In the selected sample, human inspection identified 128 real internships, 9 ambiguous cases, and 163 fake
or non-internship roles. The fake/non-internship group is larger than the accepted internship group, and the
ambiguous group indicates that some postings cannot be reliably resolved by simple keyword presence
alone. This supports the thesis claim that internship discovery is not only a storage or frontend problem; it
is also a semantic filtering and representation problem.

These results show that heterogeneous recruitment sources introduce uncertainty at the opportunity-type
level. A posting can contain internship-related words while still not being a real internship, and a posting
can be close to an internship but lack enough evidence for a confident automatic decision. Therefore, a
thesis evaluation centered only on frontend display or raw scraping volume would be incomplete. The first
empirical finding is that the data itself requires classification, normalization, and uncertainty-aware
treatment before being used for internship-oriented discovery.

\section{Semantic Structuring and Enriched Record Quality (RQ2)}

RQ2 asks how rule-based normalization, controlled skill vocabularies, and embedding-based semantic
representations can be combined into a coherent enrichment model. The frontend catalog statistics provide the output produced by this combined enrichment process. These statistics do not claim perfect
extraction accuracy; rather, they show the structure and coverage of the records made available to the
frontend after processing.

\begin{table}[H]
    \centering
    \caption{Frontend catalog statistics for enriched internship records}
    \label{tab:frontend-catalog-statistics}
    \includegraphics[width=\textwidth]{tables/Frontend catalog statistics for enriched internship records.png}
\end{table}

The frontend catalog contains 202 public internship records. The dataset includes an average internship
probability of 0.825 and a median internship probability of 0.900, which indicates that the displayed
catalog is not merely a raw collection of scraped roles but a filtered and scored output of the enrichment
pipeline. Skill extraction is also visible in the final representation: 184 out of 202 records contain extracted
skills, while 18 records contain no skill metadata. Salary information remains partially incomplete, with
106 records containing salary data and 96 records missing salary data. This is an important research
finding because it shows both the value and the boundary of heuristic structuring: many fields can be
standardized for discovery, but missing source information cannot always be recovered.

The presence of 60 manual-review records among the 202 displayed records is also significant. It shows
that the final structured representation does not only classify records; it also preserves uncertainty
indicators. This supports the thesis design choice of combining automatic enrichment with review-aware
metadata rather than forcing every posting into a fully confident decision.


\section{Internship Detection Evaluation Against Human Review (RQ4 and RQ2)}

This section evaluates internship detection against human review. It supports RQ2 by examining the
classification component within the enrichment model and supports RQ4 by showing how ambiguous
cases affect automated decision-making.

\begin{table}[H]
    \centering
    \caption{Internship classification comparison against human review}
    \label{tab:internship-classification-comparison}
    \includegraphics[width=\textwidth]{tables/Internship classification comparison against human review.png}
\end{table}

Human review is the reference for this evaluation. On the inspected 300-role sample, human review
identified 128 real internships, 9 ambiguous cases, and 163 fake or non-internship roles.

ChatGPT5.5-assisted review produced the same final distribution as the human inspection. This is reported
as 100\% sample-level agreement with the human review on this specific 300-role sample. It must not be
interpreted as proof that ChatGPT5.5 is universally 100\% accurate on all internship classification tasks.

The Transformer classified 132 roles as real internships and 168 roles as fake or non-internships. Unlike
the human and ChatGPT5.5-assisted review, the Transformer does not produce an ambiguous category.
Therefore, its performance is interpreted as binary real/fake internship detection. Against the human clear
real/fake cases, the Transformer achieved 95.19\% accuracy, 93.85\% precision, and 95.31\% recall.

Precision measures how trustworthy the system is when it classifies a role as a real internship. A high
precision score means that fewer fake or non-internship roles are incorrectly accepted as real internships.
Recall measures how many real internships the system successfully finds. A high recall score means that
fewer genuine internships are missed.

These results support a cautious interpretation. The Transformer is effective as a binary detector for real
internship identification in this inspected sample, but its lack of an ambiguous output limits its ability to
represent uncertainty. Ambiguous cases therefore remain better suited to human review or
ChatGPT5.5-assisted review, especially when the posting contains incomplete evidence or
employer-specific terminology.


\section{Discovery-Focused Frontend Evaluation (RQ3)}

RQ3 asks whether semantic enrichment improves relevance, coherence, and interpretability of internship
discovery compared with keyword-oriented or purely deterministic representations. Since no formal user
study was conducted, this section does not claim user satisfaction. Instead, it evaluates structured
discovery behavior through frontend filtering and local response evidence.

\begin{table}[H]
    \centering
    \caption{Frontend search and filter timing summary}
    \label{tab:frontend-search-filter-summary}
    \includegraphics[width=\textwidth]{tables/Frontend search and filter timing summary.png}
\end{table}

Across the tested queries, median filter times remained below 2 ms. The results show that enriched records
can be searched and filtered quickly in the tested frontend environment. This supports the argument that
preprocessing and semantic structuring can move expensive cleaning and classification work outside the
interactive user path, leaving the frontend to operate over compact structured records.

\begin{table}[H]
    \centering
    \caption{Local production frontend response speed summary}
    \label{tab:frontend-response-speed-summary}
    \includegraphics[width=\textwidth]{tables/Local production frontend response speed summary.png}
\end{table}

The local production response samples show warm median response times of 8.98 ms for the homepage,
7.88 ms for the internship API, 9.22 ms for a role page, and 14.12 ms for a single-role API response. These
figures support the practical discovery argument: the enriched dataset can be served and explored
efficiently under the tested local conditions. However, these results should be interpreted as local frontend
behavior, not as a guarantee of production performance across all networks, deployments, or hardware
environments.


\section{Operational Feasibility of the Lightweight Approach (RQ5)}

RQ5 asks about the methodological and operational limitations of lightweight semantic enrichment.
Operational feasibility is evaluated here through build logs, static generation evidence, memory usage,
catalog size, and compressed data size. These measurements are not the central research contribution, but
they indicate whether the proposed approach remains practical in a lightweight local execution setting.

\begin{table}[H]
    \centering
    \caption{Operational feasibility measurements for the lightweight frontend build}
    \label{tab:operational-feasibility-measurements}
    \includegraphics[width=\textwidth]{tables/Operational feasibility measurements for the lightweight frontend build.png}
\end{table}

The production build completed successfully with an exit code of 0. The total build time was 22.7 seconds,
including 6.0 seconds of compile time and 4.6 seconds for static page generation. The maximum build
memory was recorded as 1.819 GB. The accepted frontend dataset contained 202 records, with a JSON
size of 1.179 MB and an approximate gzip size of 231 KB.

These values support the operational feasibility of the lightweight approach under the tested local
conditions. They suggest that the system can produce and serve an enriched internship catalog without
requiring heavyweight runtime inference during ordinary browsing. At the same time, these results are
bounded by the size of the evaluated catalog and by the local testing environment. They do not prove
scalability to very large national datasets or high-traffic production deployment without further testing.


\section{Limitations and Threats to Validity (RQ5)}

Several limitations affect the interpretation of the findings. First, the classification evaluation is based on a
300-role inspected sample. Although useful for analyzing noisy scraped recruitment data, this sample
cannot represent every platform, country, domain, or language context. Second, the classification table is
treated as the selected evidence basis for this rewritten chapter; proof files containing older or inconsistent
distributions were excluded rather than mixed with it.

Third, ambiguous cases remain a core limitation. The Transformer does not produce an ambiguous
category, which means it cannot directly represent uncertainty in the same way as human or
ChatGPT5.5-assisted review. Fourth, ChatGPT5.5-assisted review is reported as sample-level agreement
with human review, not universal correctness. Fifth, the frontend timing results were produced under local
test conditions and can be affected by hardware, cache state, software configuration, and deployment
environment.

Sixth, no formal user study was conducted. Therefore, this chapter avoids claims about user satisfaction or
real student usability. The evidence supports structured discovery behavior, metadata coverage, and local
frontend responsiveness, but it does not establish how students would perceive or use the system in a
controlled user-evaluation setting. Seventh, the evidence is limited to the proof files available for this
chapter. Unsupported old benchmark metrics and derived claims without a traceable proof file were not
included.


\section{Consolidated Findings by Research Question}

The following table summarizes how the evidence addresses the research questions. The table does not introduce new statistics; it consolidates the interpretation of the evidence presented above.

\begin{table}[H]
    \centering
    \caption{Consolidated findings by research question}
    \label{tab:consolidated_findings_rq}
    \includegraphics[width=\textwidth]{tables/Consolidated findings by research question.png}
\end{table}

The consolidated findings show that the main contribution of the system is not a single benchmark number. Instead, the contribution lies in a defensible enrichment workflow: noisy scraped postings are inspected, classified, structured, enriched, and then delivered in a form that supports efficient internship-oriented discovery.


\section{Chapter Conclusion}

This chapter evaluated the current model both frontend and the transformer using the stats presented in the current Chapter 6. The results show that scraped recruitment data contains real internships, fake or non-internship roles, and ambiguous cases, confirming the need for internship-focused classification before records are presented in the frontend.

The comparison between human review, ChatGPT5.5-assisted review, and the Transformer shows strong sample-level internship detection performance. At the same time, the presence of ambiguous cases shows that uncertainty should be preserved through review indicators rather than forced into a simple real/fake decision.

The enriched catalog evidence shows that the pipeline produces structured internship records with useful metadata such as internship probabilities, extracted skills, salary information where available, links, descriptions, and review signals. Frontend search, filtering, response-time, build, and file-size evidence further show that these enriched records can support fast structured discovery under the tested local conditions.

Overall, the chapter supports the thesis argument that lightweight semantic enrichment can transform noisy scraped recruitment data into structured, interpretable, and discovery-ready internship records. The findings remain limited by the 300-role sample, the local testing environment, and the absence of a formal user study, but they provide a good evaluation of the system within the stated research scope.

